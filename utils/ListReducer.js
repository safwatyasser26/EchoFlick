

// const array = [...Array(20).keys()]; // Array with 20 values [0, 1, 2, ..., 19]
// const chunkSize = 5;

// const result = array.reduce((acc, val, index) => {
//   const chunkIndex = Math.floor(index / chunkSize);
  
//   if (!acc[chunkIndex]) {
//     acc[chunkIndex] = []; // Start a new chunk
//   }
  
//   acc[chunkIndex].push(val);
//   return acc;
// }, []);


